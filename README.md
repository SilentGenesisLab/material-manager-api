# Material Manager API

FastAPI backend for managing material files per user.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload --port 8000
```

## Environment

Create a `.env` file:

```
FILE_URL=./storage
```
